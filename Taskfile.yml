# https://taskfile.dev

version: '3'

vars:
  GREETING: Hello, World!

tasks:
  default:
    cmds:
      - task: 'build'
      - './dist/simple-userscript.exe -f example.js'

  build:
    cmds:
      - 'rm -rf dist'
      - 'mkdir dist'
      - 'go build -o dist .'